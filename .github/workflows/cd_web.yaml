name: Deploy the web
on:
  push:
    branches: [master]
    paths:
      - "dpl-dockr/**"
      - "!dpl-dockr/apps/http-server/**"
      - "!dpl-dockr/apps/ws-server/**"
      - "!dpl-dockr/dockerfile/http-server.dockerfile"
      - "!dpl-dockr/dockerfile/ws-server.dockerfile"
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout the code to machine
        uses: actions/checkout@v4
      - name: Login to DockerHub
        uses: docker/login-action@v3
        with:
          username: ${{ vars.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3
      - name: Pull Image from DockerHub
        run: docker pull tsmukesh51/dpl-dockr-web:latest || true
      - name: Build and Push to DockerHub
        uses: docker/build-push-action@v6
        with:
          context: ./dpl-dockr
          file: ./dpl-dockr/dockerfile/web.dockerfile
          push: true
          cache-from: type=registry,ref=tsmukesh51/dpl-dockr-web:latest
          tags: tsmukesh51/dpl-dockr-web:${{ github.sha }},tsmukesh51/dpl-dockr-web:latest
          cache-to: type=gha,mode=max
          secrets: DATABASE_URL=${{ secrets.NT_DATABASE_URL }}
      - name: Deploy on VM
        run: |
          mkdir -p ~/.ssh
          ssh-keyscan "${{ vars.VULTR_MYTRYING_FINGERPRINT }}" &> ~/.ssh/known_hosts
          echo "${{ secrets.SDWMUKESH_SSH_PRIVKEY }}" &> ~/.ssh/ssh_key
          chmod 600 ~/.ssh/ssh_key
          ssh -i ~/.ssh/ssh_key root@${{ vars.VULTR_MYTRYING_IPADD }} << EOF
          echo "Pulling image"
          docker pull tsmukesh51/dpl-dockr-web:${{ github.sha }} || { echo "Pull failed"; exit 1; }
          echo "Container Stopped"
          docker stop tsmukesh51/dpl-dockr-web || true
          echo "Container Removed"
          docker stop $(docker ps | grep "tsmukesh51/dpl-dockr-web" | awk '{ print $1 }') || true
          docker rm $(docker ps -a | grep "tsmukesh51/dpl-dockr-web" | awk '{ print $1 }') || true
          echo "Running New Container"
          docker run -e DATABASE_URL="${{ secrets.NT_DATABASE_URL }}" -p 3000:3000 -d tsmukesh51/dpl-dockr-web:${{ github.sha }} || { echo "Run failed"; exit 1; }
          EOF
